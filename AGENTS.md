# AGENTS.md — agent-templates（模板仓库自身）

> 本文件是任何 AI 编码代理进入本仓库时的**必读**工作说明书。
> 它约定本仓库的目录结构、模板编写规范，以及当你被要求「提取某个模板用于新项目」时的行为准则。

---

## 1. 仓库定位

本仓库是 XTsat 名下所有开源项目的**规范收敛中心**，不含业务代码，只提供「可被 agent 自动读取的规范模板」。

每个模板目录必须满足：
- **自包含**：一个 agent 只读取该目录下的 `AGENTS.md`，就能学会该类项目的**全部**约定（架构、代码规范、文档维护、提交流程）。
- **独立可用**：模板不得依赖本仓库其它文件（agent 可能只被引到单个模板文件）。

---

## 2. 目录结构（银律：新增模板遵循此结构）

```
templates/
└── <项目类型>/
    ├── AGENTS.md            # 完整规范文档（agent 必读入口）
    └── scaffold/            # 项目骨架文件（*.template 后缀，可被复制到新项目）
        ├── main.<ext>.template
        ├── metadata.yaml.template    # 若有
        ├── _conf_schema.json.template  # 若有
        ├── CHANGELOG.md.template
        └── README.md.template
```

新增模板类型时：
1. 在 `templates/<类型>/` 下写一份自包含的 `AGENTS.md`。
2. 在 `templates/<类型>/scaffold/` 下放骨架文件（后缀固定 `.template`）。
3. 在根 `README.md` 的「模板选择指南」表中登记一行。
4. 本文件第 4 节的「模板编写规范」适用于所有模板。

---

## 3. 每位模板的 `AGENTS.md` 必须包含的章节

为了让 agent 只读一个文件就能干活，每个模板的 `AGENTS.md` 建议固定以下章节顺序：

1. **项目概览** —— 这类项目是什么、核心能力、最小依赖。
2. **目录结构与架构** —— 标准文件清单 + 分层/依赖方向 + 各目录职责。
3. **技术栈与依赖** —— 语言版本、框架约束、禁止引入的依赖。
4. **核心代码规范** —— 命名、类型、注释/日志语言、错误处理、并发、防御性编程。
5. **配置与数据文件** —— schema、默认值、持久化位置。
6. **对外接口/命令清单** —— 若为插件，列指令表。
7. **文档维护规范** —— CHANGELOG / README 的**强制**同步规则。
8. **提交与发布流程** —— commit 语言、push 是否需要确认、版本号联动。
9. **最终检查清单** —— 每次任务完成前的可勾选列表。

> 各模板可按类型增删章节，但这 9 块是「让 agent 拿起来就用」的底线。

---

## 4. 模板编写规范

- **语言**：面向开发者的规范正文，默认中文（与现有 AstrBot 项目一致）；Web 应用模板中涉及用户可见文案的部分保留双语说明。
- **一致性优先**：模板正文统一约定，但要为「多种允许存在的格式」保留显式选项。例如 AstrBot 插件的 CHANGELOG 目前有三种既有格式，模板应把三种都列为「可选预设」，让新项目自选，而不是强行抹平历史差异。
- **禁止空话**：每个约定都要落到具体动作（「新增配置项 → 改这几处」而非「保持配置整洁」）。
- **含示例**：关键格式（CHANGELOG 条目、metadata.yaml、命令表）必须给出最小可用示例。
- **版本联动明确**：明确指出版本号出现在哪些文件、如何保持一致。

---

## 5.「自动提取模板」时的行为准则

当用户要求「让 agent 从这个仓库学习 astrbot-plugin 并应用」时：

1. 在 `templates/` 下找到对应类型目录。判定依据：项目名含 `astrbot_plugin_` 前缀 → 用 `astrbot-plugin`；项目是 Next.js + TypeScript → 用 `nextjs-webapp`。
2. 完整阅读该目录的 `AGENTS.md`。
3. 把 `scaffold/` 下的骨架文件复制到目标项目（去掉 `.template` 后缀）。
4. 按 AGENTS.md 的规范初始化目标项目，而不是照搬本仓库已有仓库的个别写法。
5. 如果目标项目已有同名的 `AGENTS.md` / 配置文件，**先读再合并**，不得整文件覆盖丢失既有内容（参考多会话并行开发约束）。

---

## 6. 提交约定

- 本仓库的提交信息用中文短句式，遵循 XTsat 现有仓库风格。
- 任何改动（新增模板、修改规范）**必须同步更新**根 `README.md` 的「模板选择指南」表。
- 添加或修改绑定模板前，先完整读取既有模板，保持格式与风格一致。
- 只有用户明确要求时才 commit / push。